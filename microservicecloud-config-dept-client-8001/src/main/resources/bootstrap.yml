spring:
  cloud:
    config:
      name: microservicecloud-config-dept-client #需要从GitHub上读取的资源名称，没有yml后缀名
      profile: dev  #访问的配置项
      label: master
      uri: http://config-3344.com:3344  #服务启动先去找3344号服务，通过spring cloud config 获取github 的服务地址